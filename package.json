{
  "name": "cleaning-backoffice-root",
  "version": "1.0.0",
  "description": "Root scripts to run Mopsy backend and frontend concurrently",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
